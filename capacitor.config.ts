import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "weather.id.app",
  appName: "weather_android",
  webDir: "dist/spa",
  bundledWebRuntime: false,
};

export default config;
